
```shell
curl --proto '=https' --tlsv1.2 -sSf https://tiup-mirrors.pingcap.com/install.sh | sh

# quick start all components in local machine
tiup playground

tiup cluster template > topology.yaml
```
